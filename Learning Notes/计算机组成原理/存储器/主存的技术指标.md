# 1 存储容量
$$\text{存储容量}=\text{存储单元个数}\times \text{存储字长}$$
若要用字节表示则为：
$$\text{存储容量}=\text{存储单元个数}\times \text{存储字长}/8$$

# 2 存储速度
1. 存取时间：分为读出时间和写入时间
2. 存取周期：连续两次存储器操作最小的间隔时间（存取周期通常大于存储时间）

# 3 存储器带宽
单位可以用字/秒，字节/秒，位/秒。
可以采用以下方式提高存储器带宽：
- 缩短存储周期
- 增加存储字长
- 增加存储体

